# Application_Configuration_Provider
The repository includes two Java classes. One called RunArgument for defining the run arguments, the default values for the run arguments and parsing the run arguments from a string to dedicated arguments. RunConfiguration is class for including all information regarding one run argument.
